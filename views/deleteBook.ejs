<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/node.svg" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/essentials.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <div class="heading error">Are you sure you want to delete this book?</div>

    <div class="grid-item">
      <label> <div class="heading"><%= book.title %></div></label>
      <label> <div><%= book.author %></div></label>
      <label> <div><%= book.genre %></div></label>
      <label> <div><%= book.price %></div></label>
      <label> <div><%= book.stock %> left</div></label>
    </div>

    <nav>
      <form action="/books/<%= book.book_id %>/delete" method="POST">
        <button type="submit">Delete</button>
      </form>
      <a href="/books/<%= book.book_id %>/view"><button>Go Back</button></a>
    </nav>
  </body>
</html>
